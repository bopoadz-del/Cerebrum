# Memory Log - 2026-03-15

## Render Deployment Saga

### Initial Problem
- Backend failing to deploy on Render
- Conda/micromamba causing massive 4.42GB Docker images
- Build time: 10+ minutes, export time: 9+ minutes
- Health check timeouts (Render gives up after ~5 min)

### Attempts Made
1. **Micromamba approach** (commit `6de87ca`) - Failed, syntax issues
2. **Startup optimization** (commit `acf092f`) - Made imports lazy, but image still huge
3. **Nuclear option** (commit `dbe65d0`) - Stripped Dockerfile completely:
   - Removed micromamba/conda
   - Skipped torch, transformers, chromadb, nvidia packages
   - IFC processing gracefully disabled (code handles missing ifcopenshell)

### Current State
- Dockerfile simplified to ~20 lines
- Image size reduced from 4.42GB to ~500MB
- Build time should be <2 minutes
- Waiting for Render deployment result

### Key Learnings
- Heavy ML packages (torch, transformers, nvidia) are build killers on Render
- ifcopenshell optional handling works - code has try/except for ImportError
- Fast startup requires both small image AND lazy imports

## Commit History (Today)
- `56cbef4` - Last try: Use micromamba instead of conda
- `6de87ca` - Fix micromamba command
- `61961a3` - Speed up startup - remove blocking checks
- `5d1dc00` - Increase health check start period
- `6e438b3` - Speed up startup - make Redis/db checks non-blocking  
- `acf092f` - Aggressive startup optimization - defer all heavy imports
- `dbe65d0` - Strip Dockerfile - remove micromamba, skip heavy ML packages
