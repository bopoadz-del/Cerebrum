# Heavy ML Dependencies - Pre-installed in base image
# These take 2+ GB to download, so we cache them in a base image

# Vector database
chromadb>=0.4.22

# Embeddings
sentence-transformers>=2.2.0

# ML frameworks (pulled in by above, but explicit is better)
torch>=2.0.0
transformers>=4.30.0
onnxruntime>=1.15.0

# Data science
scikit-learn>=1.3.0
scipy>=1.11.0
numpy>=1.24.0
